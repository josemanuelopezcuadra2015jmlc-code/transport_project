# Database Configuration
spring.datasource.url=jdbc:mariadb://localhost:3306/transporteDB
spring.datasource.username=root
spring.datasource.password=Smestra990112.
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080
server.servlet.context-path=/api


# JWT Configuration
jwt.secret=mySecretKeyForTransportAppThatIsLongEnoughForHS512Algorithm
jwt.expiration=86400000

# Logging
logging.level.org.springframework.security=INFO
logging.level.com.transport=DEBUG
logging.level.org.hibernate.SQL=DEBUG